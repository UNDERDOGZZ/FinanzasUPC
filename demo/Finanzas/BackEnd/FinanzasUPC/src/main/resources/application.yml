spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    password: ricarditopop356
    url: jdbc:postgresql://localhost/finanzas
    username: postgres
  jpa:
    database: POSTGRESQL
    hibernate:
      ddl-auto: update
    show-sql: 'false'
    properties.hibernate.temp.use_jdbc_metadata_defaults: false
    